# Generator Correction (iterate the generation prompt)
# Rank segments by overall pass rate using the now-trusted judge.
# Pick the worst segment × worst dimension.
# Write a targeted change to the generation prompt for the affected category/variant — don't rewrite everything at once.
